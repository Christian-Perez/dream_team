<table>
  <!-- <thead>
    <tr>
      <td>Name</td>
      <td>tag</td>
    </tr>

  </thead> -->
  <!-- <tbody> -->
    <% @teams.each do |team|%>
      <tr>
        <td><h2> <%= team.name %> </h2></td>
        <td><%= team.tag %></td>

        <td><%= link_to 'Show', team %></td>

      <!-- itterator for team.users.each do |player|-->
      </tr>

      <% team.users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
        </tr>
      <% end %>
      <!-- <%= "@roster.class: #{@roster.class}" %> -->
    <% end %>
  <!-- </tbody> -->
</table>
