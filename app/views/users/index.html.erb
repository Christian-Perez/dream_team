<h1>if created new user, should redirect to new user's page</h1>

<table>
  <!-- <thead>
    <tr>
      <td>Name</td>
      <td>tag</td>
    </tr>

  </thead> -->
  <!-- <tbody> -->
    <% @users.each do |user|%>
      <tr>
        <td><h2> <%= user.name %> </h2></td>
      </tr>

      <% user.teams.each do |team| %>
        <tr>
          <td><%= team.name %></td>
        </tr>
      <% end %>

    <% end %>
  <!-- </tbody> -->
</table>
